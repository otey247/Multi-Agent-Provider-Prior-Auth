# Foundry Hosted Agent server — Responses protocol host.
# agentserver-core 2.x provides ResponsesAgentServerHost (no agent-framework dep).
azure-ai-agentserver-core==2.0.0b6
azure-ai-agentserver-responses==1.0.0b7
# OpenAI SDK — calls the Foundry Responses API with server-side `type: mcp` tools
# (Foundry executes the MCP servers; the container opens no MCP client) and
# structured output via responses.parse(text_format=CoverageResult).
openai>=1.68.0
# Managed identity / DefaultAzureCredential (sync + azure.identity.aio)
azure-identity>=1.15.0
# Structured output schema
pydantic>=2.0
# Environment variable loading
python-dotenv>=1.0.0
# Azure Monitor OpenTelemetry (optional — App Insights bridge)
azure-monitor-opentelemetry>=1.6.0
